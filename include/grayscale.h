#pragma once

 void grayscale_triple(unsigned char* d_img, int width, int height);

void grayscale_single(unsigned char* d_img,
                      unsigned char* d_out,
                      int width,
                      int height);