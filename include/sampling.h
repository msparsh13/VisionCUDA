void downsamplefunc(unsigned char* d_in,
                      unsigned char* d_out,
                      int width,
                      int height,
                      int scale,
                      int channels);

void upsamplefunc(unsigned char* d_in,
                    unsigned char* d_out,
                    int width,
                    int height,
                    int scale,
                    int channels);